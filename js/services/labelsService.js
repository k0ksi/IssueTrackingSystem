'use strict';

angular.module('issueSystem.labels.labelsService', [])
    .factory('labelsService', [
        '$http',
        '$q',
        'BASE_URL',
        function ($http, $q, BASE_URL) {

        }
    ]);