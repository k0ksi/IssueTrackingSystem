'use strict';

angular.module('issueSystem.comments.commentsService', [])
    .factory('commentsService', [
        '$http',
        'BASE_URL',
        function ($http, BASE_URL) {

        }
    ]);